{% extends 'base.html.twig' %}

{% block title %}Soleil index{% endblock %}

{% block body %}
    <h1>Soleil index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Truc</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for soleil in soleils %}
            <tr>
                <td>{{ soleil.id }}</td>
                <td>{{ soleil.truc }}</td>
                <td>
                    <a href="{{ path('app_soleil_show', {'id': soleil.id}) }}">show</a>
                    <a href="{{ path('app_soleil_edit', {'id': soleil.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_soleil_new') }}">Create new</a>
{% endblock %}
